route:
  receiver: default
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: default
    webhook_configs:
      - url: __ALERTMANAGER_WEBHOOK_URL__
        send_resolved: true
